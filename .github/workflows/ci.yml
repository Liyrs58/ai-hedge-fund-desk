name: Offline CI

on:
  push:
    branches: [main, "quant/**"]
  pull_request:
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # npm test runs native TypeScript through --experimental-strip-types.
          # Node 20's CLI does not provide this option; it was added in Node 22.6.
          node-version: "22"
          cache: npm
      - name: Install
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Finance unit tests
        run: npm test
      - name: Paper smoke
        run: npm run paper
      - name: Production build
        run: npm run build
        env:
          SKIP_PITCH_FETCH: "1"
