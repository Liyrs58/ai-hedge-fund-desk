# Optional. Leave empty to run the paper desk on deterministic mock debates.
# If you set a provider and its key, /api/desk/run will try a live LLM and
# fall back to the mock script if the call fails.

LLM_PROVIDER=mock

# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-4-0

# GOOGLE_API_KEY=
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.0-flash

# XAI_API_KEY=
# XAI_MODEL=grok-3
